import { EventEmitter, VNode } from "../../stencil-public-runtime";
import { Scale } from "../interfaces";
import { InteractiveComponent } from "../../utils/interactive";
import { StepperItemChangeEventDetail, StepperItemEventDetail, StepperItemKeyEventDetail } from "../stepper/interfaces";
/**
 * @slot - A slot for adding custom content.
 */
export declare class StepperItem implements InteractiveComponent {
  el: HTMLCalciteStepperItemElement;
  /** is the step active */
  active: boolean;
  /** has the step been completed */
  complete: boolean;
  /** does the step contain an error that needs to be resolved by the user */
  error: boolean;
  /** is the step disabled and not navigable to by a user */
  disabled: boolean;
  /** pass a title for the stepper item */
  itemTitle?: string;
  /** pass a title for the stepper item */
  itemSubtitle?: string;
  /** pass a title for the stepper item */
  /** @internal */
  layout?: string;
  /** should the items display an icon based on status */
  /** @internal */
  icon: boolean;
  /** optionally display the step number next to the title and subtitle */
  /** @internal */
  numbered: boolean;
  /** the scale of the item */
  /** @internal */
  scale: Scale;
  disabledWatcher(): void;
  /**
   * @internal
   */
  calciteStepperItemKeyEvent: EventEmitter<StepperItemKeyEventDetail>;
  /**
   * @internal
   */
  calciteStepperItemSelect: EventEmitter<StepperItemEventDetail>;
  /**
   * @internal
   */
  calciteStepperItemRegister: EventEmitter<StepperItemEventDetail>;
  componentWillLoad(): void;
  componentDidRender(): void;
  render(): VNode;
  updateActiveItemOnChange(event: CustomEvent<StepperItemChangeEventDetail>): void;
  /** position within parent */
  private itemPosition;
  /** the latest requested item position*/
  private activePosition;
  /** the slotted item content */
  private itemContent;
  /** the parent stepper component */
  private parentStepperEl;
  private keyDownHandler;
  private renderIcon;
  private determineActiveItem;
  private registerStepperItem;
  private emitRequestedItem;
  private setItemContent;
  private getItemPosition;
}
